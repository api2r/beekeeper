# Register package methods on load

Register package methods on load

## Usage

``` r
.onLoad(...)
```

## Arguments

- ...:

  Additional arguments passed by R during package loading.

## Value

`NULL` (invisibly).
