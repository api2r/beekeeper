# Flatten a data frame method

Flatten a data frame method

## Usage

``` r
.flatten_df_data_frame(x)
```

## Arguments

- x:

  (`data.frame`) The object to return unchanged.

## Value

A `data.frame`.
