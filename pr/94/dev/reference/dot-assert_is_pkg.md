# Error if not in package

Error if not in package

## Usage

``` r
.assert_is_pkg(base_path = usethis::proj_get())
```

## Arguments

- base_path:

  The root URL of the current project.

## Value

`NULL`, invisibly.
